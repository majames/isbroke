var supervisor = require('../index.js');

describe('supervisor', function () {
	it('should not revive workers when shutting down', function () {
		supervisor.watch({
			exec: __dirname + '/app.js'
		});
	});
});